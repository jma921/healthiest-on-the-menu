{
  "name": "healthiest-on-the-menu",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@storybook/react": "^3.1.8",
    "react-scripts": "1.0.10"
  },
  "dependencies": {
    "color": "^2.0.0",
    "firebase": "^4.1.3",
    "flow-bin": "^0.50.0",
    "moment": "^2.18.1",
    "radium": "^0.19.1",
    "re-base": "^3.0.2",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-transition-group": "1.x"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "surge": "yarn build && cd build && surge --domain https://spiky-harmony.surge.sh",
    "flow": "flow",
    "storybook": "start-storybook -p 9009 -s public",
    "build-storybook": "build-storybook -s public"
  }
}
